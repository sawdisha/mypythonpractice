from sys import argv; from os.path import exists; script, from_file, to_file = argv; indata = open(from_file).read(); print(f"The input file is {len(indata)} characters long");input(f"Does the output file exists? {exists(to_file)}\nReady, hit RETURN to continue, CTRL=C to abort."); out_file = open(to_file, 'w').write(indata); print(f"Data successfully copied from {from_file} to {to_file}")
